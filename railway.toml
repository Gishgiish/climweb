[build]
# Use Railway's Dockerfile builder and point it at our production Dockerfile
builder = "Dockerfile.prod"


[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

